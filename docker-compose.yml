services:
  app:
    build: .
    image: eu-power-fairvalue-forecast:local
    working_dir: /app
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
    env_file:
      - path: .env
        required: false
    command: ["python", "main.py"]
